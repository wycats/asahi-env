ideas= [{ id = "2152d0ef-e361-49e0-b5e3-015b4f0e1c2f", title = "Infer default setup from dnf history", description = "Use Fedora/RPM transaction history to extract packages that were installed during real workstation bring-up and fold the high-signal ones into the default setup (runbook + automation).\n\nApproach:\n- Probe candidates: dnf history list, dnf history info <ID>, and dnf repoquery --userinstalled (or dnf5 equivalent).\n- Filter: exclude deps/weak deps, kernels, transient debug tools; keep user-facing/system tools (keyd, iwd/NetworkManager bits, dev tooling, diagnostics).\n- Categorize: base OS vs workstation UX vs debugging vs optional.\n- Output: generate a reviewed list in the runbook; optionally a machine-readable manifest consumed by asahi-setup.\n- Empiricism: each inclusion should cite why it exists + how to detect it’s already present.", status = "new", created_at = "2025-12-18T20:55:27.961Z", source = "user", tags = ["bootstrap", "runbook", "automation", "empiricism", "packages"], related_tasks = [] }]
